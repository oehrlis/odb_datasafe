# =============================================================================
# OraDBA Data Safe Extension - Environment Variables
# =============================================================================
# Location: Copy this file to the extension base directory as .env
#   ├── odb_datasafe/              # Extension base ($ODB_DATASAFE_BASE)
#   │   ├── .env                  # <-- Your config goes here
#   │   ├── bin/
#   │   ├── lib/
#   │   └── etc/
#   │       └── .env.example      # This template
#
# Priority: Code defaults < .env < config file < CLI arguments
#
# Usage:
#   cd $ODB_DATASAFE_BASE  # or wherever you installed odb_datasafe
#   cp etc/.env.example .env
#   vim .env  # Edit with your values
#   # Scripts auto-load from $ODB_DATASAFE_BASE/.env
# =============================================================================

# -----------------------------------------------------------------------------
# OCI Configuration
# -----------------------------------------------------------------------------
# OCI CLI configuration file and profile
OCI_CLI_CONFIG_FILE="${HOME}/.oci/config"
OCI_CLI_PROFILE="DEFAULT"
OCI_CLI_REGION="eu-frankfurt-1"

# -----------------------------------------------------------------------------
# Data Safe Configuration
# -----------------------------------------------------------------------------
# Root compartment for Data Safe operations (name or OCID)
# Can be either compartment name (e.g., "my-compartment") or OCID
# Find this in OCI Console -> Identity -> Compartments
# Example name: "my-compartment"
# Example OCID: ocid1.compartment.oc1..aaaaaaaa...
DS_ROOT_COMP="ocid1.compartment.oc1..aaaaaaaa..."

# Default lifecycle filter for target selection
# Values: CREATING|ACTIVE|INACTIVE|UPDATING|DELETING|DELETED|NEEDS_ATTENTION|FAILED
DS_LIFECYCLE="NEEDS_ATTENTION"

# Database domain (for service name construction)
DB_DOMAIN="oradba.ch"

# On-premises connector (if required)
ONPREM_CONNECTOR=""

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG|INFO|WARN|ERROR
LOG_LEVEL="INFO"

# Optional: Log to file (leave empty for console only)
LOG_FILE=""

# Color output: auto|always|never
LOG_COLOR="auto"

# -----------------------------------------------------------------------------
# Script Behavior
# -----------------------------------------------------------------------------
# Enable dry-run by default (safer for testing)
DRY_RUN="false"

# Require confirmation for destructive operations
REQUIRE_CONFIRM="true"

# -----------------------------------------------------------------------------
# Data Safe Database Credentials
# -----------------------------------------------------------------------------
# Data Safe database user (created by prereqs)
DATASAFE_USER="DS_ADMIN"

# Password (if not using secure methods)
# WARNING: Storing passwords in .env is NOT recommended
# Better: Use OCI Vault, Base64 file, or prompt
DATASAFE_PASSWORD=""

# Path to password file (base64 encoded)
DATASAFE_PASSWORD_FILE=""

# Optional CDB$ROOT password file (base64 encoded)
DATASAFE_CDB_PASSWORD_FILE=""

# Common user prefix for CDB$ROOT operations
COMMON_USER_PREFIX="C##"

# -----------------------------------------------------------------------------
# Optional: Project-specific Overrides
# -----------------------------------------------------------------------------
# Uncomment and customize as needed

# Default compartment name (if not using OCID)
# COMPARTMENT_NAME="my-datasafe-compartment"

# Default listener port
# LISTENER_PORT="1521"

# Service protocol
# SERVICE_PROTOCOL="TCP"

# -----------------------------------------------------------------------------
# Advanced: Performance Tuning
# -----------------------------------------------------------------------------
# Parallel execution limit (for batch operations)
# MAX_PARALLEL_JOBS="5"

# OCI CLI timeout (seconds)
# OCI_TIMEOUT="300"

# Retry attempts for transient failures
# MAX_RETRIES="3"

# =============================================================================
# End of configuration
# =============================================================================
