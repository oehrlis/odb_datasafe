# ------------------------------------------------------------------------------
# OraDBA - Oracle Database Infrastructure and Security, 5630 Muri, Switzerland
# ------------------------------------------------------------------------------
# Name.......: release.yml
# Author.....: Stefan Oehrli (oes) stefan.oehrli@oradba.ch
# Editor.....: Stefan Oehrli
# Date.......: 2026.04.08
# Version....: 0.3.0
# Purpose....: Release workflow - build and publish tarball on version tag push
# Notes......: Triggered by v*.*.* tags or manual workflow_dispatch with version
# Reference..: https://github.com/oehrlis/odb_datasafe
# License....: Apache License Version 2.0, January 2004 as shown
#              at http://www.apache.org/licenses/
# ------------------------------------------------------------------------------
name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 0.1.0)'
        required: true
        type: string

jobs:
  build-and-release:
    name: Build and Publish Tarball
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck bats shfmt
          npm install -g markdownlint-cli

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/v}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Release version: $VERSION"

      - name: Verify VERSION file
        run: |
          FILE_VERSION="$(tr -d '[:space:]' < VERSION)"
          if [ "$FILE_VERSION" != "${{ steps.version.outputs.version }}" ]; then
            echo "VERSION file ($FILE_VERSION) does not match release version (${{ steps.version.outputs.version }})"
            exit 1
          fi

      - name: Validate version format
        run: make check-version

      - name: Run CI checks
        run: make ci

      - name: Generate release notes
        id: release_notes
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          RELEASE_NOTES_FILE="doc/release_notes/v${VERSION}.md"

          if [ -f "$RELEASE_NOTES_FILE" ]; then
            echo "Using detailed release notes from $RELEASE_NOTES_FILE"
            cp "$RELEASE_NOTES_FILE" release_notes.md
          else
            echo "Release notes file not found, generating generic notes"
            cat > release_notes.md <<EOF
          # odb_datasafe v${VERSION}

          Oracle Data Safe Management Scripts

          ## What's Changed

          See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.

          ## Assets

          - **odb_datasafe-${VERSION}.tar.gz**: Distribution tarball
          - **odb_datasafe-${VERSION}.tar.gz.sha256**: Checksum file

          ## Installation

          \`\`\`bash
          tar -xzf odb_datasafe-${VERSION}.tar.gz
          cd odb_datasafe
          \`\`\`

          ## Documentation

          - [Quick Reference](https://github.com/${{ github.repository }}/blob/main/doc/quickref.md)
          - [Installation Guide](https://github.com/${{ github.repository }}/blob/main/doc/install_datasafe_service.md)
          - [OCI IAM Policies](https://github.com/${{ github.repository }}/blob/main/doc/oci-iam-policies.md)
          EOF
          fi

      - name: Verify build artifacts exist
        run: |
          # Same glob as the release step below. The tarball prefix comes from
          # the name: field in .extension, so it must not be hardcoded here.
          VERSION="${{ steps.version.outputs.version }}"
          shopt -s nullglob
          tarballs=(dist/*-"${VERSION}".tar.gz)
          checksums=(dist/*-"${VERSION}".tar.gz.sha256)
          if [ ${#tarballs[@]} -eq 0 ] || [ ${#checksums[@]} -eq 0 ]; then
            echo "No build artifacts for version ${VERSION} in dist/"
            ls -la dist/ 2>/dev/null || echo "dist/ does not exist"
            exit 1
          fi
          for f in "${tarballs[@]}" "${checksums[@]}"; do
            if [ ! -s "$f" ]; then
              echo "Build artifact is empty: $f"
              exit 1
            fi
          done
          echo "Build artifacts present:"
          ls -la dist/
        shell: bash

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }}
          body_path: release_notes.md
          # Publish immediately. Never leave a draft behind - the repository
          # accumulated seven orphaned drafts before this was made explicit.
          draft: false
          prerelease: false
          # Abort instead of publishing a release without its tarball.
          fail_on_unmatched_files: true
          files: |
            dist/*-${{ steps.version.outputs.version }}.tar.gz
            dist/*-${{ steps.version.outputs.version }}.tar.gz.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
