# OraDBA Data Safe Extension v0.12.0

**Release Date:** 2026-02-17

## Overview

Version 0.12.0 introduces OraDBA extension etc-hook integration for automatic
environment and alias loading.

## Highlights

- **Enabled extension etc hooks** in `.extension`:
  - `load_env: true`
  - `load_aliases: true`
- **Added `etc/env.sh`** for idempotent Data Safe environment initialization.
- **Added `etc/aliases.sh`** for Data Safe helper aliases.
- **Updated `datasafe_env.sh`** to load `etc/env.sh` and `etc/aliases.sh`
  directly when sourced.
- **Improved bash/ksh compatibility** for hook path resolution and PATH updates.

## Details

### Added

- Extension metadata
  - `.extension` now enables OraDBA optional etc hooks using
    `load_env: true` and `load_aliases: true`.
- `etc/env.sh`
  - Initializes `ODB_DATASAFE_BASE`, `DATASAFE_BASE`, and `DATASAFE_SCRIPT_BIN`.
  - Updates `PATH` idempotently when the bin directory exists.
- `etc/aliases.sh`
  - Adds convenience aliases:
    - `dshelp`
    - `dsversion`
    - `dsconcheck`

### Fixed

- `bin/datasafe_env.sh`
  - Now sources `etc/env.sh` and `etc/aliases.sh` and applies their settings.
  - Keeps fallback defaults/aliases when hook files are not present.
- `etc/env.sh`
  - Uses shell-safe path resolution for both `bash` and `ksh` when sourced
    directly or through `datasafe_env.sh`.

## Testing

```bash
# Syntax checks for new hook files
bash -n etc/env.sh etc/aliases.sh
```

## Breaking Changes

None.

## See Also

- [Changelog](../../CHANGELOG.md)
- [Release note v0.11.2](v0.11.2.md)
